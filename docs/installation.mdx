---
slug: /installation
sidebar_position: 3
title: Installation
---

There are multiple installation methods. Pick the one that best fits your needs.

## Bundler

Add the following line to your Gemfile:

```ruby
gem "convenient_service", "~> 0.1.0"
```

And then run:

```bash
bundle install
```

## Without Bundler

Execute the command below:

```bash
gem install convenient_service -v 0.1.0
```

:::info

Convenient Service follows the [Semantic Versioning](https://semver.org/) standard.

:::
