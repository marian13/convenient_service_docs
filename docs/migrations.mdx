---
slug: /migrations
sidebar_position: 1
title: Migrations
---

### From `v0.20.0` to higher.

Just update Gemfile and run `bundle install`.

### From `v0.19.1` to `v0.20.0`.

Follow the guides below:

- [Deprecations: Reassignments](/deprecations/reassignments).

- [Deprecations: Some `require` dependency methods](/deprecations/some_require_dependency_methods).

### From `v0.16.0` to `v0.19.1`.

Just update Gemfile and run `bundle install`.

### Lower versions.

Check the [CHANGELOG](https://github.com/marian13/convenient_service/blob/main/CHANGELOG.md).
