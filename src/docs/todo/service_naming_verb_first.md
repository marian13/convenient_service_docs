# Service Naming: Start with a Verb

TODO: Document the convention that Convenient Service service names should start with a verb.

Examples:
- `CheckNetwork`, `SpawnDevServer`, `WaitServerHealthcheck`
- `RemoveFolder`, `TouchFolder`, `SaveUrl`, `ResolveDistPath`

Verbs make the intent clear and distinguish services from plain data objects or modules.

Cover: which verbs to prefer (`Resolve` vs `Get`, `Build` vs `Create`, etc.) and edge cases.
